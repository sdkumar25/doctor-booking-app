import React from 'react'
import AppointmentSuccessReviewContainer from '../containers/AppointmentSuccessReviewContainer'

const AppointmentSuccessReview = props => (
  <div>
    <AppointmentSuccessReviewContainer />
  </div>
)

export default AppointmentSuccessReview